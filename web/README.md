# Web 前端计划

首版前端采用 Vue 3 + Vite + TypeScript，提供 PC Web 工作台：

- 学生：任务查看、成果上传、进度查看、评价反馈。
- 教师：任务配置、评价指标、提交核查、教师复核、报表导出。
- 管理员：用户、课程、班级、系统配置、审计查看。

阶段 0 暂不引入 Node 依赖，避免在治理初始化阶段增加 lockfile 和 LoongArch 依赖风险。进入阶段 2 前再创建完整前端工程并提交锁文件。
